// Generated by CoffeeScript 1.6.1
(function() {
  var askWhichFriend, bindClickEvents, buidConversation, buidpage, build, conversation, display_login_info, facebookLogin, setAndBindPageSizes;

  conversation = [];

  $(document).ready(function() {
    return buidpage();
  });

  buidpage = function() {
    return bindClickEvents();
  };

  bindClickEvents = function() {
    return $("#firstButton").click(function() {
      return facebookLogin();
    });
  };

  facebookLogin = function() {
    var some_condition_is_met;
    some_condition_is_met = true;
    if (some_condition_is_met) {
      return askWhichFriend();
    } else {
      return display_login_info();
    }
  };

  askWhichFriend = function() {
    return $(".welcome").addClass("moveUp");
  };

  display_login_info = function() {
    var loginFailed, tryLogin;
    $("#firstButton").fadeOut(100);
    tryLogin = function() {
      return null;
    };
    return loginFailed = function() {
      return null;
    };
  };

  buidConversation = function() {
    var $convo, message, _i, _len, _results;
    $convo = $(".CONVO");
    _results = [];
    for (_i = 0, _len = conversation.length; _i < _len; _i++) {
      message = conversation[_i];
      _results.push($convo.append(build(message)));
    }
    return _results;
  };

  build = function(message) {
    var $m;
    return $m = $("<div/>", {
      id: "" + message.id,
      "class": "" + message.direction,
      text: "" + message.text
    });
  };

  setAndBindPageSizes = function() {
    var setSize;
    setSize = function() {
      var w;
      w = $(window).width();
      return $(".CONVO").width();
    };
    $(window).bind('resize', function() {
      return setSize();
    });
    return setSize();
  };

}).call(this);
